# Normalise toutes les fins de ligne en LF dans le dépôt.
# ArgoCD clone ce repo dans un conteneur Linux : des CRLF Windows casseraient
# le rendu Helm et l'exécution du Makefile côté WSL/Linux.
* text=auto eol=lf
